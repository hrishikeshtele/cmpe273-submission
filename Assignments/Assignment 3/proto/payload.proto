syntax = "proto3";

message Request {
   string filename = 1;
   bytes data = 2;
}

message Response {
   bool status = 1;
}

service SendAdaptivePayload {
  rpc data_transfer(Request) returns (Response) {}
}